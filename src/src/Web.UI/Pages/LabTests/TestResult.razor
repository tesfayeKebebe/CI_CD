@using Web.UI.Server.Models.LabTestResults
@using Microsoft.AspNetCore.Components
@inherits TestResultBase
<RadzenTemplateForm TItem="TestResultDetail" Data="model">
    <div class="flex-row m-bottom">
        <div class="flex-col col-Padding">
            <div class="label">Name</div>
            <RadzenTextArea Name="description" @bind-Value="model.Description" Style="width: 100%; height: 100%"></RadzenTextArea>
            <div class="flex-row">
                <div class="flex-col col-Padding">
                    <RadzenRequiredValidator Component="description" Text="Description is required"></RadzenRequiredValidator>
                </div>
            </div>
        </div>
    </div>

       <div class="flex-row" style="margin-top: 20px">
                <div class="flex-col col-Padding">
                    @*                <div class="button">
                    <RadzenButton Text="Back" ButtonStyle="ButtonStyle.Dark" Click="@(() => OnBack())"></RadzenButton>
                </div>*@
                    <div onclick="@OnBack" class="flex-col  col-Padding ">
                        <img class="button-image" src="image/back.png" alt=""/>
                    </div>
                </div>
                @if (!IsFinish)
                {
                    <div class="flex-col col-Padding">
                        @*                    <div class="button">
                        <RadzenButton Text="Confirm" ButtonStyle="ButtonStyle.Success" Click="@(() => OnSubmit())"></RadzenButton>
                    </div>*@
                        <div onclick="@OnSave" class="flex-col  col-Padding">
                            <span class="primary-button">Save</span>  
                        </div>
                    </div>
                }
                  @if (IsFinish)
                  {
                      <div class="flex-col col-Padding">
                          @*                    <div class="button">
                                        <RadzenButton Text="Confirm" ButtonStyle="ButtonStyle.Success" Click="@(() => OnSubmit())"></RadzenButton>
                                    </div>*@
                          <div onclick="@OnFinish" class="flex-col  col-Padding">
                              <span class="primary-button">Finish</span>
                          </div>
                      </div>
                  }
                
            </div>


</RadzenTemplateForm>